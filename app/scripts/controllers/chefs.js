'use strict';

/**
 * @ngdoc function
 * @name myappApp.controller:ChefsCtrl
 * @description
 * # ChefsCtrl
 * Controller of the myappApp
 */
angular.module('myappApp')
  .controller('ChefsCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
